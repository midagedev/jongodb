{
  "name": "@jongodb/memory-server",
  "version": "0.0.0-development",
  "description": "Node.js launcher for jongodb in-memory server",
  "keywords": [
    "mongodb",
    "mongodb-memory-server",
    "jongodb",
    "integration-test",
    "jest",
    "vitest"
  ],
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./jest": "./dist/jest.js",
    "./vitest": "./dist/vitest.js"
  },
  "typesVersions": {
    "*": {
      "jest": [
        "./dist/jest.d.ts"
      ],
      "vitest": [
        "./dist/vitest.d.ts"
      ]
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "npm run build",
    "test": "node --test dist/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.17.57",
    "mongodb": "^6.21.0",
    "typescript": "^5.9.2"
  }
}
