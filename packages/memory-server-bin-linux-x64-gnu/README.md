# @jongodb/memory-server-bin-linux-x64-gnu

Platform binary package for `@jongodb/memory-server`.

Target:
- OS: Linux
- CPU: x64
- libc: glibc

This package is consumed via optional dependency from `@jongodb/memory-server`.
